name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: yarn add eslint --dev
    - name: Run ESLint
      run: yarn run eslint yourfile.js && yarn run eslint *.js
